// config.js — minimal shared state.
//   auto=false → manual: flight reacts to live audio (music).
//   auto=true  → self-animating: ignores audio, driven by Perlin motion.
const VJ = {
  auto: false,
  sensitivity: 1.0,
};
